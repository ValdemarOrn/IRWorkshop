More features that can be added:

Adjust Phase band frequency bands by a multiplier
On output stage (combined)
	6 Band parametric EQ
	Output Stage control

Stereo version of the phase bands - remove it from the spectrum stage instead
stereo enhancing - investigate
mono->stereo trick - add a few tapped delayed signal to the stereo spectrum, low passed, delayed and panned







			<CheckBox IsChecked="{Binding UseLeftChannel}" IsEnabled="{Binding LeftChannelAvailable}" />
			<Label>Right</Label>
			<CheckBox IsChecked="{Binding UseRightChannel}" IsEnabled="{Binding RightChannelAvailable}" />